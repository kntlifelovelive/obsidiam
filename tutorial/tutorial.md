

```
mv !(@(keep1|keep2|logs|target1)) target1/

## This is  my test command line 
mv !(@(bubu.txt|dudu.txt|teti15)) teti15/ 

```

```bash

find . -type f -iname "*mp3" -print0 | xargs -0 mpv --no-video 

find . -type f -iname "*mp3" -exec mv -t path 

 shopt -s extglob 
 

```